#include <cstdlib>  // atof
#include <iostream>
using std::abs;  
using std::cout;
using std::endl;

const double EPSILON = 0.0001;

int main(int argc, char* argv[]) {
    int start = 0;
    double val = atof(argv[1]);
    int upper = val * val;
    int lower;

    while (upper > val) {
        if ()
        {
            /* code */
        }
        
    }
}
